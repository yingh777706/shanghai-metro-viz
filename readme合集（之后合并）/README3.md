# 分工3：地铁客流空间地图可视化

## 分工说明

- **成员**：lks（吕可珊）[@kaishuiliang](https://github.com/kaishuiliang)
- **方向**：空间可视化
- **核心职责**：站点线路底图构建、客流热力图、OD流向图、动态交互可视化

## 目录结构

```
分工3_空间可视化/
├── 图片/                    # 静态可视化成果（300DPI高清PNG）
│   ├── 图3-1 全时段站点客流热力图.png
│   ├── 图3-2 分时段客流空间对比图.png
│   ├── 图3-3 工作日周末客流差异图.png
│   └── 图3-4 全时段OD客流流向图.png
├── 交互地图/                # 可交互网页可视化
│   └── 上海地铁客流交互地图.html
├── 源代码/                  # Python可视化脚本
│   ├── 03_空间数据构建.py
│   ├── 04_全时段客流热力图.py
│   ├── 05_分时段对比图.py
│   ├── 06_工作日周末差异.py
│   ├── 07_OD流向图.py
│   └── 08_交互地图.py
├── 空间数据/                # 构建好的空间数据
│   └── station_flow_geo.geojson
└── README.md                # 本说明文档
```

## 成果说明

### 静态图片

| 文件名 | 说明 |
|--------|------|
| 图3-1 全时段站点客流热力图.png | 302个站点全天总客流空间分布，点大小+颜色双编码，标注TOP10核心站点 |
| 图3-2 分时段客流空间对比图.png | 早高峰/晚高峰/工作日日均/周末日均四宫格对比，统一色阶便于横向比较 |
| 图3-3 工作日周末客流差异图.png | 双向色阶差值图（工作日-周末），红色=通勤主导站，蓝色=休闲主导站 |
| 图3-4 全时段OD客流流向图.png | TOP50客流对的贝塞尔弧线图，弧线粗细对应客流量级，展示城市主要通勤走廊 |

### 交互地图

`上海地铁客流交互地图.html`：基于Folium构建的可交互网页，双击即可在浏览器打开。支持地图缩放拖拽，点击任意站点可弹出该站全天、早高峰、晚高峰的详细客流数据。

### 空间数据

`station_flow_geo.geojson`：带客流属性的站点空间数据（WGS84坐标系），包含站点ID、名称、经纬度、全天总客流、早高峰客流、晚高峰客流、工作日日均、周末日均等字段，可直接用于QGIS、Kepler.gl等GIS工具。

## 数据来源

本分工的可视化基于分工1（数据工程）预处理后的聚合数据：
- `station_flow_agg.csv`：站点级多维度客流聚合表
- `od_flow_agg.csv`：TOP50 OD客流对聚合表

> 注：CSV数据文件因体积较大，按仓库 `.gitignore` 规则不纳入版本控制。如需原始聚合数据，请联系分工1成员获取，或基于 `processed_data` 中的10分钟粒度原始数据运行 `源代码/01_站点客流聚合.py` 和 `02_OD客流聚合.py` 重新生成。

## 运行环境

- Python 3.10+
- 依赖库：pandas、geopandas、matplotlib、contextily、pyproj、shapely、folium

```bash
pip install pandas geopandas matplotlib contextily pyproj shapely folium
```

## 运行步骤

1. 确保 `station_flow_agg.csv` 和 `od_flow_agg.csv` 在当前目录
2. 依次运行脚本：
   ```bash
   python 源代码/03_空间数据构建.py      # 生成 station_flow_geo.geojson
   python 源代码/04_全时段客流热力图.py   # 生成图3-1
   python 源代码/05_分时段对比图.py       # 生成图3-2
   python 源代码/06_工作日周末差异.py     # 生成图3-3
   python 源代码/07_OD流向图.py           # 生成图3-4
   python 源代码/08_交互地图.py           # 生成交互HTML
   ```

## 技术要点

- **底图**：使用高德地图浅色瓦片（自带地铁线路标注），通过 `contextily` 叠加
- **坐标系**：原始数据 WGS84 (EPSG:4326)，叠加底图时自动转换为 Web墨卡托 (EPSG:3857)
- **OD弧线**：使用二次贝塞尔曲线生成弧线，避免直线重叠，提升可读性
- **交互地图**：基于 Folium + CartoDB 底图，CircleMarker 编码客流量级
