# 分工2 · 业务分析 任务清单

> 规范依据：[docs/项目路径与协作规范.md](../docs/项目路径与协作规范.md)

| 项 | 内容 |
| --- | --- |
| **仓库地址** | https://github.com/yingh777706/shanghai-metro-viz.git |
| **角色身份** | hy（@yingh777706）· 业务分析负责人（兼仓库 owner） |
| **工作目录** | `code/分工2/`、`分工2_统计分析/` |
| **行动边界** | 不修改 `code/common.py` 接口；不碰分工1/3/4/5 的目录与脚本 |

## 已完成（无需重复）

- 统计脚本 + 表1~表6 已入库
- OD 平均出行距离 **16.06 km** 已跑通

## 待办任务（按优先级）

- [x] **P0-1 修 OUT_DIR**：`code/分工2/statistical_analysis.py` 里 `OUT_DIR = PROJECT_ROOT / "输出结果" / "统计分析"` 改为 `PROJECT_ROOT / "分工2_统计分析"`，废弃 `输出结果/`（见规范第 3 条「运行即交付」）。
- [x] **P0-2 结论归档**：`统计分析结论.md`（含 16.06km 结论）现停在 `readme合集/`，需移入 `分工2_统计分析/`。
- [x] **P1-1 结论补人工解读**：在 `统计分析结论.md` 补 3~5 句业务归因（为什么周五最高、8 月为何逐月攀升、人民广场为何断层第一），不要只留脚本自动拼的数字。
- [x] **P1-2 提数据需求单**：向分工1 明确提出「平均出行时长（OD 表补 startTime 差）、站点→线路映射、核心城区/郊区分类」三张数据需求的格式与用途，写入 `分工2_统计分析/数据需求.md` 或直接发群。

## 完成标准

- 重跑 `python code/分工2/statistical_analysis.py --skip-od` 后，结果直接落回 `分工2_统计分析/`，与交付目录一致；结论含人工解读。

## 提交提醒

1. 提交前 `python -m py_compile code/分工2/statistical_analysis.py` 通过。
2. 不提交大 CSV（`.gitignore` 已处理，只保留结果表）。
3. commit 格式：`分工2: 修 OUT_DIR 指向 分工2_统计分析`。
4. 提交后 push 到 `main`。
