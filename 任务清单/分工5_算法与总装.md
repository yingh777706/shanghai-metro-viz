# 分工5 · 算法 + 总装 任务清单

> 规范依据：[docs/项目路径与协作规范.md](../docs/项目路径与协作规范.md)

| 项 | 内容 |
| --- | --- |
| **仓库地址** | https://github.com/yingh777706/shanghai-metro-viz.git |
| **角色身份** | ycl 袁楚帘（@guaovo）· 算法 + 项目总装负责人 |
| **工作目录** | `code/分工5/`、`分工5_数据挖掘/`、`报告/`、根 `README.md` |
| **行动边界** | 总装时可读全员成果，但改其他分工的交付物需经本人确认；不修改 `code/common.py` 接口 |

## 已完成（无需重复）

- K-Means 聚类 + ARIMA 预测两套脚本、结果、说明文档已入库

## 待办任务（按优先级）

- [ ] **P0-1 修 OUT_DIR**：`code/分工5/kmeans_station_clustering.py` 与 `arima_station_forecast.py` 里 `输出结果/聚类|预测` 改为 `分工5_数据挖掘/聚类|预测`（见规范第 3 条「运行即交付」）。
- [ ] **P0-2 文档迁回**：`readme合集/数据挖掘说明文档.md` 迁回 `分工5_数据挖掘/`。
- [ ] **P0-3 更新根 README 过时段落**：根 `README.md` 的「使用说明/结果对照表/项目结构/当前进展」仍是旧路径与旧结构（`code/kmeans_station_clustering.py` 等），需按新目录结构重写，并把分工3、分工4 的进展勾选为 `[x]`。
- [ ] **P1-1 清空 `readme合集/`**：确认各分工迁回自己的文档后，删除 `readme合集/` 目录。
- [ ] **P1-2 K 值论证增强**：加 GMM / 层次聚类对比，或在报告明确「K=5 是任务约束、轮廓系数 K=3 更高」的取舍，避免被问「为什么不是 K=3」。
- [ ] **P1-3 预测进阶（可选）**：SARIMAX 引入天气/星期哑变量，或 LightGBM 特征工程，目标从「诚实承认不如基线」进阶到「真正赢过基线」。
- [ ] **P2-1 总装**：整合分工2/3/4 成果，校验全项目数据口径一致性，补汇报 PPT，并稿期末报告（`报告/`）。

## 完成标准

- 根 README 与新目录结构一致、无过期路径；`readme合集/` 清空；PPT 与并稿报告就绪。

## 提交提醒

1. 提交前 `python -m py_compile code/分工5/*.py` 通过。
2. 不提交大 CSV；PPT/报告 docx 可入库。
3. commit 格式：`分工5: 修 OUT_DIR + 更新根 README` 等。
4. 提交后 push 到 `main`，并在群内发布「总装完成」清单。
