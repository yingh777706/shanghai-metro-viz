# 分工1 · 数据工程 任务清单

> 规范依据：[docs/项目路径与协作规范.md](../docs/项目路径与协作规范.md)

| 项 | 内容 |
| --- | --- |
| **仓库地址** | https://github.com/yingh777706/shanghai-metro-viz.git |
| **角色身份** | pzq（@Panzhongqiong）· 数据工程负责人 |
| **工作目录** | `code/分工1/`（新建）、`docs/`、`data/` |
| **行动边界** | 不修改 `code/common.py` 的既有接口（可调用）；不碰分工2/3/4/5 的目录与脚本 |

## 已完成（无需重复）

- 数据清洗完成，4 个标准文件已交付（位于 `docs/processed_data/`）
- 已写 `数据预处理说明文档.md` 与 `清洗运行日志.txt`

## 待办任务（按优先级）

- [ ] **P0-1 入库清洗脚本 + 校验脚本**：当前仓库只有文档和日志，没有任何清洗代码，无法复现。把清洗脚本和 `data_check.py` 放入 `code/分工1/`，并把硬编码的绝对路径（历史日志里的 `C:\Users\yzcw_\Desktop\...`）改成基于 `common.PROJECT_ROOT`。
- [ ] **P0-2 迁移文档回 `docs/`**：`数据预处理说明文档.md`、`清洗运行日志.txt` 现被挪到 `readme合集/`，按规范迁回 `docs/`。
- [ ] **P1-1 补 dataset manifest**：新增 `docs/dataset_manifest.md`，列出每个标准文件的字段、行数、md5，供下游自检数据是否对齐。
- [ ] **P1-2 文档字段与实际对齐**：`workday_calendar.csv` 实际含 `weekday` 列，但文档第 4 节字段说明漏写，需补上。

## 配合项

- **响应分工2 的数据需求单**（分工2 会提）：出行时长字段、站点→线路映射、核心城区/郊区分类。如短期无法提供，需明确回复「缺哪一步、预计何时能给」。

## 完成标准

- `code/分工1/` 下可独立复跑清洗流程；`docs/` 下文档与数据字段一致；`readme合集/` 中属于分工1 的文件已迁出。

## 提交提醒

1. 提交前 `python -m py_compile` 通过。
2. **不要提交数据文件**（`data/`、`docs/processed_data/` 已 gitignore）。
3. commit 格式：`分工1: 入库清洗脚本与校验脚本` 等，一次提交聚焦一件事。
4. 提交后 push 到 `main`，并在群内告知分工2/5（他们依赖你的数据底座）。
