# 分工1 · 数据工程 任务清单

> 规范依据：[docs/项目路径与协作规范.md](../docs/项目路径与协作规范.md)

| 项 | 内容 |
| --- | --- |
| **仓库地址** | https://github.com/yingh777706/shanghai-metro-viz.git |
| **角色身份** | pzq（@Panzhongqiong）· 数据工程负责人 |
| **工作目录** | `docs/`、`data/`（清洗脚本不入库） |
| **行动边界** | 不修改 `code/common.py` 的既有接口（可调用）；不碰分工2/3/4/5 的目录与脚本 |

## 已完成（无需重复）

- 数据清洗完成，标准数据已交付分工2（含 `station_info` 增强版、`station_line_map`、`station_area_class`、`od_travel_time` 等新表）
- 已写 `数据预处理说明文档.md` 与 `清洗运行日志.txt`

## 待办任务（按优先级）

- [ ] **P0-1 数据说明入库（替代清洗脚本）**：清洗脚本 `clean_data.py`/`data_check.py` **不入库**（数据已交付分工2，硬编码绝对路径不必再改，见规范第 3 条铁律1 的例外说明）。把分支 `潘忠琼` 里的 `readme合集（之后合并）/README_补充数据说明.md` 移到 `docs/dataset_manifest.md`，列出新增/增强表的字段与用途。
- [ ] **P0-2 迁移文档回 `docs/`**：`数据预处理说明文档.md`、`清洗运行日志.txt` 现被挪到 `readme合集/`，按规范迁回 `docs/`。
- [ ] **P1-1 文档字段与实际对齐**：`workday_calendar.csv` 实际含 `weekday` 列，但文档第 4 节字段说明漏写，需补上；`dataset_manifest.md` 补充每个标准文件的字段、行数、md5，供下游自检。

## 配合项

- **已响应分工2 的数据需求单**：出行时长字段、站点→线路映射、核心城区/郊区分类已随新数据交付。

## 完成标准

- `docs/` 下文档与交付数据字段一致；`docs/dataset_manifest.md` 覆盖全部新增/增强表；`readme合集/` 中属于分工1 的文件已迁出。

## 提交提醒

1. 提交前自检文档路径与链接无失效。
2. **不要提交数据文件与清洗脚本**（`data/`、`docs/processed_data/` 已 gitignore；`code/分工1/` 脚本不入库）。
3. commit 格式：`分工1: 数据说明入库 + 文档迁回 docs` 等，一次提交聚焦一件事。
4. 提交后 push 到 `main`，并在群内告知分工2/5（他们依赖你的数据底座）。
