# 分工4 · 统计可视化 任务清单

> 规范依据：[docs/项目路径与协作规范.md](../docs/项目路径与协作规范.md)

| 项 | 内容 |
| --- | --- |
| **仓库地址** | https://github.com/yingh777706/shanghai-metro-viz.git |
| **角色身份** | ly（@lyy11234）· 统计可视化负责人（统一视觉风格的责任方） |
| **工作目录** | `分工4_统计可视化/`（含 `源代码/viz_style.py`） |
| **行动边界** | 可直接改 `viz_style.py` 供全员复用；修改其他分工的图需经对应分工确认 |

## 已完成（无需重复）

- 8 张统计图 + 8 个脚本 + 统一风格模块 `viz_style.py`（含异常日处理）

## 待办任务（按优先级）

- [ ] **P0-1 修 README 过期路径**：`分工4_统计可视化/README.md` 里引用 `code/数据挖掘/输出结果/聚类/表2_聚类结果表.csv` 已失效，改成 `分工5_数据挖掘/聚类/表2_聚类结果表.csv`（否则图4-5 会误判「缺标签」而降级）。
- [ ] **P0-2 统一数据口径标注**：README 写「人民广场进出站合计约 3500 万」，分工2 写「进站 1800 万」——进站 vs 进出站合计需显式标注口径，避免报告口径打架（见规范第 6 条）。
- [ ] **P1-1 输出《可视化规范》**：把 `viz_style.py` 的配色/字体/版式整理成一份简短规范（可并入 `viz_style.py` 顶部 docstring），推动分工3、分工5 的图也复用，真正落地「统一视觉风格」。

## 完成标准

- README 无过期路径、口径标注清晰；`viz_style.py` 成为全项目唯一风格来源。

## 提交提醒

1. 提交前 `python -m py_compile 分工4_统计可视化/源代码/*.py` 通过。
2. commit 格式：`分工4: 修 README 过期路径 + 口径标注`。
3. 提交后 push 到 `main`，并在群内通知分工3/5 复用 `viz_style.py`。
