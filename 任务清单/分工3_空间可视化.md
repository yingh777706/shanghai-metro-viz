# 分工3 · 空间可视化 任务清单

> 规范依据：[docs/项目路径与协作规范.md](../docs/项目路径与协作规范.md)

| 项 | 内容 |
| --- | --- |
| **仓库地址** | https://github.com/yingh777706/shanghai-metro-viz.git |
| **角色身份** | lks 吕可珊（@kaishuiliang）· 空间可视化负责人 |
| **工作目录** | `code/分工3/`、`分工3_空间可视化/` |
| **行动边界** | 不修改 `code/common.py` 接口；可视化风格以分工4 的 `viz_style.py` 为准；不碰其他分工目录 |

## 已完成（无需重复）

- 4 张静态图 + 1 个 folium 交互地图 + 1 个 geojson + 脚本 03~08

## 待办任务（按优先级）

- [ ] **P0-1 脚本路径重构**：`code/分工3/` 下脚本里的纯相对路径（如 `gpd.read_file("station_flow_geo.geojson")`）换成基于 `common.PROJECT_ROOT`，否则换目录运行即报错（见规范第 3 条）。
- [ ] **P0-2 README 迁回**：`readme合集/README3.md` 迁回 `分工3_空间可视化/README.md`。
- [ ] **P1-1 补聚合脚本 01/02**：`station_flow_agg.csv`、`od_flow_agg.csv` 的生成脚本 `01_站点客流聚合.py`、`02_OD客流聚合.py` 缺失，需补进 `code/分工3/`；或改为直接读分工1 标准表，去掉对这两个中间文件的依赖。
- [ ] **P1-2 标题中文化 + 风格统一**：图标题/图例由英文改为中文，配色复用分工4 的 `viz_style.py`（当前英文标题与分工2/4/5 的中文明显割裂）。
- [ ] **P1-3 魔法数字与交互地图**：`markersize=客流/3000`、`/2000`、`/5000` 等改成基于分位数；交互地图补中文 popup、图例，并对 radius 做上下限裁剪。

## 完成标准

- 在 `code/分工3/` 任意目录都能复跑出图；图风格与分工4 一致；README 在 `分工3_空间可视化/` 下。

## 提交提醒

1. 提交前 `python -m py_compile code/分工3/*.py` 通过。
2. geojson/PNG 可入库；中间大 CSV 不入库。
3. commit 格式：`分工3: 脚本路径重构 + 标题中文化`。
4. 提交后 push 到 `main`。
